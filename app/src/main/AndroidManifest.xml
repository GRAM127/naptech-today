<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.naptech.today">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Naptech">
        <activity android:name=".account.AccountEditActivity"></activity>
        <activity android:name=".study.StudyActivity" />
        <activity android:name=".study.StudyAddActivity" />
        <activity android:name=".study.StudyPopupActivity" />
        <activity android:name=".schedule.SchedulePopupActivity" />
        <activity android:name=".account.AccountActivity" />
        <activity android:name=".schedule.ScheduleAddActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>